<% if target.errors.any? %>
<div class="well sidebar-nav">
  <ul class="nav nav-list" style="color:#9d261d;">
    <li class="nav-header"><h3><%= pluralize(target.errors.count, "error") + " prohibited this record from being saved:" %></h3></li>
    <% target.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
  </ul>
</div>
<% end %>
